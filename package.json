{
  "name": "validate-einvoice-action",
  "version": "1.7.0",
  "private": true,
  "description": "GitHub Action that validates EN 16931 e-invoices (UBL, CII, Factur-X PDF) in CI.",
  "type": "module",
  "license": "MIT",
  "author": "Attestwire",
  "homepage": "https://attestwire.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/attestwire/validate-einvoice-action.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "ncc build src/index.js -o dist --license licenses.txt",
    "test": "node --test",
    "all": "npm run build && npm test"
  },
  "dependencies": {
    "@actions/core": "1.11.1",
    "@actions/glob": "^0.7.0",
    "@attestwire/en16931": "0.12.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.45.0"
  }
}
